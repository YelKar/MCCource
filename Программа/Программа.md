# План работы
- [Платформа](#платформа)
  1. [Знакомство с базой C/C++](#1-знакомство-с-базой-cc)
  1. [Знакомство с Arduino](#2-знакомство-с-arduino)
  1. [Знакомство с драйверами моторов](#3-знакомство-с-драйверами-моторов)
  1. [Сборка платформы и знакомство с Колёсами Илона](#4-сборка-платформы-и-знакомство-с-колёсами-илона)
  1. [Знакомство с ESP32](#5-знакомство-с-ESP32)
  1. [Сборка платформы на ESP](#6-сборка-платформы-на-ESP)
- [Сайт](#сайт)

## Общее
> ### Базы ЯП
>> Объясняем базовые конструкции языков программирования
>> #### Консоль
>>> Вкратце рассказываем про то, что с программой можно взаимодействовать через консоль. \
>>> Рассказываем про ввод и вывод.
>> #### Типизация
>>> Рассказываем про виды типизации.
>>> Говорим, что **статическая типизация** заставляет определить тип переменной перед её использованием и запрещает менять его до окончания работы программы. \
>>> **Динамическая типизация**, напротив, позволяет подставлять в переменную значения любых типов.
>> #### Переменные
>>> **Объявление переменной** - создание имени переменной в пространстве имён и выделение памяти под неё. \
>>> **Присваивание переменной** - установка значения в переменной. \
>>> **Инициализация переменной** - объявление, а затем присваивание переменной (в одной операции).
>> #### Базовые операторы (`+`, `-`, `*`, `/` (`//`), `%`)
>> 



[//]: # (Ссылки на примеры)
[hello_world]: files/1/helloWorld.c
[1]: files/1

[hello_world_arduino]: files/2/helloWorld/helloWorld.ino
[blink]: files/2/blink/blink.ino
[blink2]: files/2/blink2/blink2.ino
[LEDs]: files/2/leds/leds.ino
[withBtn]: files/2/ledWithBtn/ledWithBtn.ino

[PS2_example]: ../Arduino/JoystickPS2/JoystickPS2.ino

[PWM_LED_BUILTIN]: files/5/PWM_LED_BUILTIN.ino
[PWM_LED]: files/5/PWM_LED.ino
[Arduino_code]: files/6/Arduino_code.ino
[ESP32_code]: files/6/ESP32_code.ino

## Платформа
> ### 1. Знакомство с базой C/C++
>> Рассказываем про самые базы Си и C++.
>> Делаем это как можно проще и понятнее,
>> проводя параллели с Питоном.
>> Пишем **_[Hello, world!][hello_world]_** и [несколько простых алгоритмов][1]. Даем простые задания, чтобы смогли попрактиковаться писать код.
>>
>> #### Базовые типы
>>> `int`, `double`, ~~`float`~~, `char`, `bool`, Массивы.
>>>
>>> _`short`, `long`, `long long`, `signed`, `unsigned` - Возможно_
>> #### Переменные и выражения
>> ```c++
>> type var;       // Объявление переменной
>> var = exp;      // Присваивание значения переменной
>> type var = exp; // Инициализация переменной
>> 
>> // Операторы
>> int a = 5; // Инициализация -> 5
>> a + 2      // Сложение -> 7
>> a - 2      // Вычитание -> 3
>> a * 2      // Умножение -> 10
>> a / 2      // Целочисленное деление -> 2
>> a / 2.0    // Вещественное деление -> 2.5
>> a % 2      // Взятие остатка от деления -> 1
>> -a         // Унарный минус -> -5
>> +a         // Унарный плюс -> 5
>> 
>> // Булева логика
>> exp1 == exp2 // Равно
>> exp1 != exp2 // Не равно
>> exp1 > exp2  // Больше
>> exp1 < exp2  // Меньше
>> exp1 >= exp2 // Больше или равно
>> exp1 <= exp2 // Меньше или равно
>> 
>> // Инкремент и декремент
>> int a = 0;
>> int b;
>> b = a++;     // a == 1, b == 0
>> b = ++a;     // a == 2, b == 2
>> b = --a;     // a == 1, b == 1
>> b = a--;     // a == 0, b == 1
>> b = a++ + 10 // a == 1, b == 10
>> b = --a + 10 // a == 0, b == 10
>> 
>> // Составное присваивание
>> var += exp  // var = var + exp
>> var -= exp  // var = var - exp
>> var *= exp  // var = var * exp
>> var /= exp  // var = var / exp
>> var %= exp  // var = var % exp
>> ```
>> #### Блоки и области видимости
>> ```c++
>> int a = 5,
>>     b = 10;
>> {
>>     int a = 7;
>>     b += 2;
>>     // Вывод a: 7
>>     // Вывод b: 12
>> }
>> // Вывод a: 5
>> // Вывод b: 12
>>```
>> #### Базовые конструкции
>> ```c++
>> // Условная конструкция
>> if (exp) block;
>> if (exp) block1; else block2;
>> if (exp1) block1; else if (exp2) block2; else block3;
>> exp ? returnIfTrue : returnIfFalse  // Тернарный оператор возвращает значение в зависимости от условия
>> 
>> // while
>> while (exp) block;
>> do block while (exp);
>> 
>> // for
>> for (exp1; exp2; exp3) block;
>> for (;;) block;
>> for (int var : array) block;
>> ```
>> #### Функции
>> ```c++
>> returnType funcName(arg1Type arg1, arg2Type arg2);  // Объявление функции
>> returnType funcName(arg1Type arg1, arg2Type arg2) block;  // Определение функции
>> 
>> // Пример
>> int func(int a, int b) {
>>     a *= a;
>>     b *= b;
>>     return a + b;
>> }
>> ```
>> #### Макросы (Кратко)
>> ```c++
>> #define macroVar value
>> #define macroFunc(arg1, arg2) (arg1 + arg2)
>> int a = macroVar * 2;         // value * 2
>> double b = macroFunc(5.5, 7); // 5.5 + 7
>> ```
> ### 2. Знакомство с Arduino
>> Начинаем работать на Arduino. Пишем [Hello, world!][hello_world_arduino]
>> Соберем простую схему со светодиодами для демонстрации работы.
>>> - [Встроенный светодиод][blink] ([Другой вариант][blink2])
>>> - [С внешними светодиодами][LEDs] - [Схема](https://wokwi.com/projects/369334501923983361)
>>> - [С кнопкой][withBtn] - [Схема](https://wokwi.com/projects/369335916027354113)
>> ##### _Для создания схем будем использовать сайт [WokWi](https://wokwi.com)_
>>
>> #### Базовый принцип работы
>> ```c++
>> void setup(); // Выполняется один раз в начале программы
>> void loop();  // Является циклом. Повторяется на протяжение всей программы
>> ```
>>
>> #### Взаимодействие с пинами
>> ```c++
>> void pinMode(pin, mode);       // Конфигурация пина
>> 
>> int digitalRead(pin);          // Цифровое чтение
>> void digitalWrite(pin, val);   // Цифровая запись
>>
>> // Функции для работы с аналоговыми (A0-A6) и ШИМ пинами
>> int analogRead(pin);  // Возвращает значение от 0 до 1023
>> void analogWrite(pin, val); // val - значение от 0 до 255
>>```
>> #### Встроенные макросы
>>> `LED_BUILTIN` // Вывод на встроенный светодиод \
>>> `OUTPUT`|`INPUT`|`INPUT_PULLUP` \
>>> `HIGH`|`LOW`
>> #### Последовательный порт
>> ```c++
>> void Serial::begin(freq);  // Запуск последовательного порта
>> 
>> int Serial::print(anyType);  // Вывод в последовательный порт
>> int Serial::println(anyType);  // Вывод в последовательный порт с переводом на следующую строку
>> int Serial::println();  // Перевод на следующую строку
>> ```
>> #### Другие встроенные функции
>> ```C++
>> void delay(unsigned long ms);  // Задержка
>> long map(value, fromLow, fromHigh, toLow, toHigh); // Изменение диапазона
>> // map(5, 0, 10, 10, 20) -> 15
>> long random(long max);           // Случайное значение от 0 до max (Включительно)
>> long random(long min, long max); // Случайное значение от min до max (Включительно)
>> #define max(a, b)    (a > b ? a : b)  // Максимальное значение 
>> #define min(a, b)    (a < b ? a : b)  // Минимальное значение
>> #define abs(x)       (x > 0 ? x : -x) // Модуль
>> #define round(x)     (x >= 0 ? (long)(x + 0.5) : (long)(x - 0.5)) // Округление
>> #define radians(deg) (deg * DEG_TO_RAD) // Градус в Радиан
>> #define degrees(rad) (rad * RAD_TO_DEG) // Радиан в Градус
>> ```
> ### 3. Знакомство с драйверами моторов
>> Рассказываем про опасность индукционного тока для Ардуино.
>> Максимально загружаем и запугиваем ребят информацией про сложность управления моторами. \
>> **Но** у нас есть драйвера, которые сильно облегчают задачу!
>> Подключаем к Arduino драйвера моторов.
>> Объясняем принцип работы драйверов моторов.
> ### 4. Сборка платформы и знакомство с _Колёсами Илона_
>> **Собираем платформу на _Колёсах Илона_. \
>> Пишем для нее программу на _Arduino_. \
>> Разбираемся с принципом работы _Колёс Илона_**\
>> _Управление будет осуществляться через джойстик от PS2_\
>> [Пример][PS2_example]
> ### 5. Знакомство с ESP32
>> Знакомимся с [ШИМ каналами][PWM_LED_BUILTIN] на ESP32.
>> Создаем простую схему со светодиодами [с использованием ШИМ][PWM_LED]
>> ```c++
>> int ledcSetup(int channel, int freq, int resolution_bits);
>> void ledcWrite(int chan, int duty);
>> ```
> ### 6. Сборка платформы на ESP
>> Заменяем Arduino на ESP32. \
>> _Исправляем_ код под ESP32
>> #### Например, [такой][Arduino_code] код на Arduino переводим в [аналогичный][ESP32_code] на ESP32

## Сайт
[🡹](#План-работы)

### 1. Определение MVP, база по проге, Git
1. Выяснить, кто что умеет/знает  // говорим | 5 мин
2. Определить MVP (что мы посчитаем за приемлимый результат, в общих словах), чуть обсудить архитектуру  // интерактив | 20 мин
3. Дать базу по проге (абстрактно): переменные, ветвления, циклы, функции, классы  // говорим, но живо и быстро | 10 мин
4. Разделиться по направлениям: дизайн/верстка, апи  // говорим + интерактив | 5 мин
5. Каждое направление расходится накидывать своё MVP  // интерактив | 10 мин 
6. Обсуждаем результаты обсуждений  // говорим + интерактив | 10 мин
7. перерыв 5 мин
8. Дать базу по Git и code style  // говорим 15 мин
9. Каждый делает "Hello, World" в своей ветке, учимся делать коммит, пуш и ПР  // интерактив 30-45 мин
10. Стендап

### 2. Введение в технологии и первые шаги к сайту
1. Введение про HTML, HTTP и работу интернета
2. Ставим задачи на урок
3. Дизайн на бумаге | Основы Python, FastAPI/Django/Flask (что-то одно)
4. перерыв 5 мин
5. HTML, CSS, `<div>` | Ребята пилят первый API
6. Стендап

статья про основы фронта (описан стек и фреймворки) https://habr.com/ru/companies/otus/articles/674748/

### 3. 
1. Ставим задачи на урок
2. Ребята пилят дизайн   | Продолжаем изучать фреймворк
3. Углубляемся в вёрстке | Ребята пилят ручки
4. Стендап

### 4-5. 
Как и в уроках 2-3 две группы работают параллельно.
Первый час препод (Тима или Дима) преимущественно с одной группой, потом - с другой.

### 6. Катим
лучше, чтобы все преподы тут были
1. Узнаём, кому что остаолось доделать
2. Фигачим час
3. Сверяемся, перерыв
4. Только причёсываем (без новых фич) ещё 30 минут
5. Релиз
6. Обратная связь
